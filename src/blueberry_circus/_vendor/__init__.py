"""Vendored third-party-ish (same-owner) code, shipped so the wheel is
self-contained. `nanarch_certify` here is a verbatim mirror of the canonical
canonical package; the resolver in `blueberry_circus.certify` adds this directory
to sys.path so `import nanarch_certify` resolves here. See PROVENANCE.md."""
