---
name: Bug report
about: Something is wrong (a crash, a wrong number, a certificate that lies)
labels: bug
---

**What happened**

**Minimal reproduction**

```python
```

**Expected**

**Environment**
- OS / Python / numpy versions:
- Backend (`numpy` / `rust` / `jax`):
- `pytest` summary line on your machine:

**If a reported number is wrong:** attach the certificate bundle JSON. The
recorded numbers and rule are the ground truth for the discussion.
